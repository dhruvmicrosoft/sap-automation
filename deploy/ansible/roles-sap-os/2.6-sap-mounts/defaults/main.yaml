# Copyright (c) Microsoft Corporation.
# Licensed under the MIT License.

---

use_AFS:                               "{{ NFS_provider == 'AFS' }}"

scs_virtual_hostname:                  "{{ custom_scs_virtual_hostname | default(sap_sid | lower ~ 'scs' ~ scs_instance_number ~ 'cl1', true) }}"

nfs_fs_type:                           "{% if use_eit_for_afs and ansible_distribution_major_version | int != 16 %}aznfs{% else %}nfs4{% endif %}"
...
